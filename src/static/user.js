const form=document.querySelector('#task-form');const statusBox=document.querySelector('#status');const message=document.querySelector('#status-message');const fill=document.querySelector('#progress-fill');const progress=document.querySelector('#progress-label');const result=document.querySelector('#result');const resultText=document.querySelector('#result-text');let poller;
async function request(url,options){const r=await fetch(url,options);if(!r.ok)throw new Error('Không thể thực hiện yêu cầu.');return r.json()}
function draw(data){statusBox.classList.remove('hidden');message.textContent=data.message;fill.style.width=`${data.progress}%`;progress.textContent=`${data.progress}%`;if(data.result){result.classList.remove('hidden');resultText.textContent=data.result;clearInterval(poller)}}
form.addEventListener('submit',async e=>{e.preventDefault();clearInterval(poller);result.classList.add('hidden');const file=document.querySelector('#file').files[0];try{const data=await request('/api/executions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:document.querySelector('#message').value,file_name:file?.name||null})});draw(data);poller=setInterval(async()=>{try{draw(await request(`/api/executions/${data.id}`))}catch{clearInterval(poller)}},550)}catch(err){message.textContent=err.message;statusBox.classList.remove('hidden')}});